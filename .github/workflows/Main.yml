name: WebBrowser

on:
  workflow_dispatch:

jobs:
  WebBrowser:
    name: WebBrowser
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # IP CHECK: Prints the standard datacenter tracking parameters for your reference
      - name: Inspect Runner Public IP & ISP Info
        run: |
          echo "========================================================"
          echo "🔍 TRACKING OUTBOUND TRAFFIC ENVIRONMENT IP..."
          echo "========================================================"
          curl -s https://ipinfo.io/json
          echo -e "\n========================================================"

      # CHROMIUM ENGINE CONFIGURATION:
      # Restored to the working Chromium base image on port 3000, 
      # combined with the Chromebook user-agent profile identity.
      - name: Start Isolated Chromium Docker Browser
        run: |
          docker run -d \
            --name=virtual-browser \
            --ipc=host \
            --shm-size="6gb" \
            -e PUID=1000 \
            -e PGID=1000 \
            -e TZ=Asia/Kolkata \
            -p 3000:3000 \
            lscr.io/linuxserver/chromium:latest \
            --user-agent="Mozilla/5.0 (X11; CrOS x86_64 14541.0.0) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"

      # EXPOSES TUNNEL & HOLDS RUNNER SECURELY FOR 6 HOURS USING LOCALHOST.RUN (LHR.LIFE)
      - name: Create Web Connection
        run: |
          echo "========================================================"
          echo "🚀 SPARKING ENGINE & INITIALIZING WEBSTREAM VIA LOCALHOST.RUN..."
          echo "========================================================"
          
          touch stream_tunnel.log
          
          # Establish background tunnel connection parameters via your original localhost.run setup
          ssh -o StrictHostKeyChecking=no -R 80:localhost:3000 nokey@localhost.run > stream_tunnel.log 2>&1 &
          
          # Exactly 10 seconds to handshake before pulling the url link
          echo "⏳ Waiting 10 seconds for streaming connection to secure..."
          sleep 10
          
          # Extracts the newly generated lhr.life URL from the logs
          LIVE_URL=$(grep -o 'https://[^ ]*\.lhr\.life' stream_tunnel.log | head -n 1)
          
          if [ -z "$LIVE_URL" ]; then
            echo "❌ Link creation paused. Connection log output:"
            cat stream_tunnel.log
            exit 1
          fi
          
          echo "========================================================"
          echo "🎉 DOCKER VIRTUAL CHROMIUM BROWSER RUNNING SUCCESSFULLY!"
          echo "👉 LIVE BROWSER URL: $LIVE_URL"
          echo "========================================================"
          echo "⏳ SYSTEM LOCK ACTIVE: Holding execution environment live..."
          echo "========================================================"
          
          # Forced 6-hour pause block so the runner stays completely alive without showing logs
          sleep 21600
          
